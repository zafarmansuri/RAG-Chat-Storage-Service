/*
 * Placeholder asset kept public for compatibility with existing tests and
 * local tooling. The active UI behavior is bundled in index.html.
 */
window.RagChatStorageShell = window.RagChatStorageShell || {};
